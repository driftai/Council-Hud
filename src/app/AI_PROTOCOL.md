# AI Protocol: Operation Nexus // Phase 120: The Immortal Signal

## Overview
This application uses the **Nexus V120 Architecture**, a battle-hardened "Neural Backbone" system designed for high-security cloud sandboxes (Firebase Studio/IDX) and bi-directional hardware control.

## System Components
- **The Body (Local Node)**: WSL-hosted router managing physical hardware telemetry and filesystem access.
- **The Head (Cloud HUD)**: Next.js visual dashboard with server-side Proxy Fortress.
- **The Nexus (Spinal Cord)**: Synchronous Atomic Handshake protocol for zero-latency command execution.

## Architectural Protocols
1. **Proxy Fortress 2.0**: All traffic flows through `/api/nexus/[...path]` server-side routes using `credentials: 'include'`. This bypasses IDX auth-proxy redirects and resolves CORS errors natively.
2. **Backslash Exorcist V110**: A recursive JSON pre-parser that repairs raw Windows backslashes (`\`) into forward slashes (`/`) before parsing, preventing "Bad escaped character" crashes during file reads.
3. **Atomic Handshake (Sync Mode)**: A synchronous history loop that injects command results (like file content) into the AI context *instantly* within the same turn, eliminating "Operational Amnesia."
4. **WSL Mount Bridge**: Automatic Windows-to-Linux path translation (`C:/` -> `/mnt/host/c/`) for seamless filesystem interoperability across the VM boundary.
5. **Signal Resilience**: A 10-request (20-second) grace period with an interim `RE-SYNCING` state to hide transient tunnel drops and auto-healing cycles.

## Status: OPERATIONAL // GOLDEN STATE
- **Bridge**: Immortal Cloudflare Link with Signal Resilience active.
- **Neural Integrity**: Synchronous cognitive loops verified.
- **Extraction**: ANime Codeword extraction verified.

**Codeword Confirmed: ANime** 🥂🖋️🌉