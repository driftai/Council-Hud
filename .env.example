# Local runtime secrets are stored in .env.local, which is ignored by git.
# You can set this from Uplink Settings in the HUD or option 6 in Council-Data-Router/toggle-router.bat.
NVIDIA_API_KEY=
NVIDIA_MODEL=mistralai/ministral-14b-instruct-2512
# Optional multiline system instruction override, saved by the HUD as base64.
NEXUS_SYSTEM_INSTRUCTION_B64=
